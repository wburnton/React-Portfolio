import React, { useState } from 'react'; 
import PortfolioContainer from './PortContainer';

export default function Header() { 
    //const [header, setHeader] = useState([]); 
    return ( 
        <header>
          <PortfolioContainer/>
        </header>
    )

    
};

